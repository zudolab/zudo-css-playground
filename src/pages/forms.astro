---
import BaseLayout from '../layouts/base-layout.astro';
import HtmlPreview from '../components/html-preview.tsx';
---

<BaseLayout title="Forms" activeCategory="forms">
  <h1 class="text-heading font-bold mb-vsp-lg">Form Patterns</h1>
  <p class="text-body text-muted mb-vsp-xl">6 form/input variations built with strict design tokens.</p>

  <div class="space-y-vsp-xl">

    <HtmlPreview
      client:visible
      title="1. Simple Login Form"
      html={`
<div class="form-1">
  <h2 class="form-1__title">Sign In</h2>
  <form class="form-1__form">
    <div class="form-1__field">
      <label class="form-1__label" for="login-email">Email</label>
      <input class="form-1__input" type="email" id="login-email" placeholder="you@example.com" />
    </div>
    <div class="form-1__field">
      <label class="form-1__label" for="login-password">Password</label>
      <input class="form-1__input" type="password" id="login-password" placeholder="••••••••" />
    </div>
    <button class="form-1__button" type="submit">Sign In</button>
  </form>
</div>
      `}
      css={`
.form-1 {
  max-width: 360px;
  margin: 0 auto;
}

.form-1__title {
  font-size: var(--font-lg);
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--fg);
}

.form-1__form {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.form-1__field {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
}

.form-1__label {
  font-size: var(--font-sm);
  font-weight: 500;
  color: var(--fg-muted);
}

.form-1__input {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  padding: var(--space-sm) var(--space-md);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--bg);
  color: var(--fg);
  outline: none;
  transition: border-color 0.15s ease;
}

.form-1__input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px hsl(220 70% 50% / 0.15);
}

.form-1__input::placeholder {
  color: var(--border);
}

.form-1__button {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  font-weight: 600;
  padding: var(--space-sm) var(--space-md);
  border: none;
  border-radius: var(--radius);
  background: var(--accent);
  color: var(--bg);
  cursor: pointer;
  transition: background 0.15s ease;
}

.form-1__button:hover {
  background: var(--accent-hover);
}

.form-1__button:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
      `}
    />

    <HtmlPreview
      client:visible
      title="2. Contact Form"
      html={`
<div class="form-2">
  <h2 class="form-2__title">Contact Us</h2>
  <form class="form-2__form">
    <div class="form-2__field">
      <label class="form-2__label" for="contact-name">Name</label>
      <input class="form-2__input" type="text" id="contact-name" placeholder="Your name" />
    </div>
    <div class="form-2__field">
      <label class="form-2__label" for="contact-email">Email</label>
      <input class="form-2__input" type="email" id="contact-email" placeholder="you@example.com" />
    </div>
    <div class="form-2__field">
      <label class="form-2__label" for="contact-message">Message</label>
      <textarea class="form-2__textarea" id="contact-message" rows="4" placeholder="How can we help?"></textarea>
    </div>
    <button class="form-2__button" type="submit">Send Message</button>
  </form>
</div>
      `}
      css={`
.form-2 {
  max-width: 480px;
  margin: 0 auto;
}

.form-2__title {
  font-size: var(--font-lg);
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--fg);
}

.form-2__form {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.form-2__field {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
}

.form-2__label {
  font-size: var(--font-sm);
  font-weight: 500;
  color: var(--fg-muted);
}

.form-2__input,
.form-2__textarea {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  padding: var(--space-sm) var(--space-md);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--bg);
  color: var(--fg);
  outline: none;
  transition: border-color 0.15s ease;
}

.form-2__textarea {
  resize: vertical;
  min-height: 100px;
}

.form-2__input:focus,
.form-2__textarea:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px hsl(220 70% 50% / 0.15);
}

.form-2__input::placeholder,
.form-2__textarea::placeholder {
  color: var(--border);
}

.form-2__button {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  font-weight: 600;
  padding: var(--space-sm) var(--space-md);
  border: none;
  border-radius: var(--radius);
  background: var(--accent);
  color: var(--bg);
  cursor: pointer;
  transition: background 0.15s ease;
  align-self: flex-start;
}

.form-2__button:hover {
  background: var(--accent-hover);
}

.form-2__button:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
      `}
    />

    <HtmlPreview
      client:visible
      title="3. Inline Search Bar"
      html={`
<div class="form-3">
  <form class="form-3__bar">
    <input class="form-3__input" type="search" placeholder="Search…" />
    <button class="form-3__button" type="submit">Search</button>
  </form>
</div>
      `}
      css={`
.form-3 {
  max-width: 480px;
  margin: 0 auto;
}

.form-3__bar {
  display: flex;
  gap: 0;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
  transition: border-color 0.15s ease;
}

.form-3__bar:focus-within {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px hsl(220 70% 50% / 0.15);
}

.form-3__input {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  padding: var(--space-sm) var(--space-md);
  border: none;
  background: var(--bg);
  color: var(--fg);
  flex: 1;
  outline: none;
}

.form-3__input::placeholder {
  color: var(--border);
}

.form-3__button {
  font-family: system-ui, sans-serif;
  font-size: var(--font-sm);
  font-weight: 600;
  padding: var(--space-sm) var(--space-md);
  border: none;
  border-left: 1px solid var(--border);
  background: var(--bg-subtle);
  color: var(--fg);
  cursor: pointer;
  transition: background 0.15s ease, color 0.15s ease;
  white-space: nowrap;
}

.form-3__button:hover {
  background: var(--accent);
  color: var(--bg);
}
      `}
    />

    <HtmlPreview
      client:visible
      title="4. Stacked Labels Form"
      html={`
<div class="form-4">
  <h2 class="form-4__title">Create Account</h2>
  <form class="form-4__form">
    <div class="form-4__field">
      <label class="form-4__label" for="stacked-first">First Name</label>
      <input class="form-4__input" type="text" id="stacked-first" />
    </div>
    <div class="form-4__field">
      <label class="form-4__label" for="stacked-last">Last Name</label>
      <input class="form-4__input" type="text" id="stacked-last" />
    </div>
    <div class="form-4__field">
      <label class="form-4__label" for="stacked-email">Email Address</label>
      <input class="form-4__input" type="email" id="stacked-email" />
    </div>
    <div class="form-4__field">
      <label class="form-4__label" for="stacked-pass">Password</label>
      <input class="form-4__input" type="password" id="stacked-pass" />
    </div>
    <button class="form-4__button" type="submit">Create Account</button>
  </form>
</div>
      `}
      css={`
.form-4 {
  max-width: 400px;
  margin: 0 auto;
}

.form-4__title {
  font-size: var(--font-lg);
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--fg);
}

.form-4__form {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.form-4__field {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
}

.form-4__label {
  font-size: var(--font-sm);
  font-weight: 600;
  color: var(--fg);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.form-4__input {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  padding: var(--space-sm) var(--space-md);
  border: 2px solid var(--border);
  border-radius: var(--radius);
  background: var(--bg-subtle);
  color: var(--fg);
  outline: none;
  transition: border-color 0.15s ease, background 0.15s ease;
}

.form-4__input:focus {
  border-color: var(--accent);
  background: var(--bg);
}

.form-4__button {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  font-weight: 600;
  padding: var(--space-sm) var(--space-md);
  border: none;
  border-radius: var(--radius);
  background: var(--accent);
  color: var(--bg);
  cursor: pointer;
  margin-top: var(--space-xs);
  transition: background 0.15s ease;
}

.form-4__button:hover {
  background: var(--accent-hover);
}

.form-4__button:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
      `}
    />

    <HtmlPreview
      client:visible
      title="5. Floating Labels Form"
      html={`
<div class="form-5">
  <h2 class="form-5__title">Floating Labels</h2>
  <form class="form-5__form">
    <div class="form-5__field">
      <input class="form-5__input" type="text" id="float-name" placeholder=" " />
      <label class="form-5__label" for="float-name">Full Name</label>
    </div>
    <div class="form-5__field">
      <input class="form-5__input" type="email" id="float-email" placeholder=" " />
      <label class="form-5__label" for="float-email">Email Address</label>
    </div>
    <div class="form-5__field">
      <input class="form-5__input" type="password" id="float-pass" placeholder=" " />
      <label class="form-5__label" for="float-pass">Password</label>
    </div>
    <button class="form-5__button" type="submit">Continue</button>
  </form>
</div>
      `}
      css={`
.form-5 {
  max-width: 400px;
  margin: 0 auto;
}

.form-5__title {
  font-size: var(--font-lg);
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--fg);
}

.form-5__form {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.form-5__field {
  position: relative;
}

.form-5__input {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  width: 100%;
  padding: var(--space-md) var(--space-md) var(--space-xs);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--bg);
  color: var(--fg);
  outline: none;
  transition: border-color 0.15s ease;
}

.form-5__input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px hsl(220 70% 50% / 0.15);
}

.form-5__label {
  position: absolute;
  left: var(--space-md);
  top: 50%;
  transform: translateY(-50%);
  font-size: var(--font-md);
  color: var(--fg-muted);
  pointer-events: none;
  transition: top 0.2s ease, font-size 0.2s ease, color 0.2s ease;
  background: var(--bg);
  padding: 0 4px;
}

.form-5__input:focus + .form-5__label,
.form-5__input:not(:placeholder-shown) + .form-5__label {
  top: 0;
  transform: translateY(-50%);
  font-size: var(--font-sm);
  color: var(--accent);
}

.form-5__button {
  font-family: system-ui, sans-serif;
  font-size: var(--font-md);
  font-weight: 600;
  padding: var(--space-sm) var(--space-md);
  border: none;
  border-radius: var(--radius);
  background: var(--accent);
  color: var(--bg);
  cursor: pointer;
  margin-top: var(--space-xs);
  transition: background 0.15s ease;
}

.form-5__button:hover {
  background: var(--accent-hover);
}

.form-5__button:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
      `}
    />

    <HtmlPreview
      client:visible
      title="6. Settings Form with Toggle Switches"
      html={`
<div class="form-6">
  <h2 class="form-6__title">Settings</h2>
  <form class="form-6__form">
    <div class="form-6__row">
      <div class="form-6__info">
        <span class="form-6__name">Email Notifications</span>
        <span class="form-6__desc">Receive updates via email</span>
      </div>
      <label class="form-6__toggle">
        <input class="form-6__checkbox" type="checkbox" checked />
        <span class="form-6__track"></span>
      </label>
    </div>
    <div class="form-6__row">
      <div class="form-6__info">
        <span class="form-6__name">Dark Mode</span>
        <span class="form-6__desc">Use dark theme</span>
      </div>
      <label class="form-6__toggle">
        <input class="form-6__checkbox" type="checkbox" />
        <span class="form-6__track"></span>
      </label>
    </div>
    <div class="form-6__row">
      <div class="form-6__info">
        <span class="form-6__name">Auto-save</span>
        <span class="form-6__desc">Save changes automatically</span>
      </div>
      <label class="form-6__toggle">
        <input class="form-6__checkbox" type="checkbox" checked />
        <span class="form-6__track"></span>
      </label>
    </div>
    <div class="form-6__row">
      <div class="form-6__info">
        <span class="form-6__name">Two-Factor Auth</span>
        <span class="form-6__desc">Extra security for your account</span>
      </div>
      <label class="form-6__toggle">
        <input class="form-6__checkbox" type="checkbox" />
        <span class="form-6__track"></span>
      </label>
    </div>
  </form>
</div>
      `}
      css={`
.form-6 {
  max-width: 420px;
  margin: 0 auto;
}

.form-6__title {
  font-size: var(--font-lg);
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--fg);
}

.form-6__form {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.form-6__row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-md) 0;
  border-bottom: 1px solid var(--border);
}

.form-6__row:last-child {
  border-bottom: none;
}

.form-6__info {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.form-6__name {
  font-size: var(--font-md);
  font-weight: 500;
  color: var(--fg);
}

.form-6__desc {
  font-size: var(--font-sm);
  color: var(--fg-muted);
}

.form-6__toggle {
  position: relative;
  cursor: pointer;
  flex-shrink: 0;
}

.form-6__checkbox {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
}

.form-6__track {
  display: block;
  width: 44px;
  height: 24px;
  background: var(--border);
  border-radius: 12px;
  position: relative;
  transition: background 0.2s ease;
}

.form-6__track::after {
  content: "";
  position: absolute;
  top: 2px;
  left: 2px;
  width: 20px;
  height: 20px;
  background: var(--bg);
  border-radius: 50%;
  transition: transform 0.2s ease;
  box-shadow: 0 1px 3px hsl(220 25% 15% / 0.2);
}

.form-6__checkbox:checked + .form-6__track {
  background: var(--accent);
}

.form-6__checkbox:checked + .form-6__track::after {
  transform: translateX(20px);
}

.form-6__checkbox:focus-visible + .form-6__track {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
      `}
    />

  </div>
</BaseLayout>
